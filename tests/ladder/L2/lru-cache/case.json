{
  "id": "l2-lru-cache",
  "level": 2,
  "kind": "data-structure",
  "prompt": "Create a Python module with a class `LRUCache(capacity)` supporting `get(key)` (returns None when missing) and `put(key, value)`. When the cache exceeds capacity, evict the least recently used entry. Both get and put count as a use. A capacity of 0 must store nothing.",
  "target_file": "solution.py",
  "grader": "python3 -m pytest -q --tb=short",
  "timeout_seconds": 300,
  "grader_timeout_seconds": 120
}
