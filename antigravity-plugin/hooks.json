{
  "hooks": {
    "pre_tool_use": [
      {
        "matcher": "write_file|execute_url|command",
        "command": "python3",
        "args": ["{{PLUGIN_DIR}}/../mcp_server.py", "--hook", "routing-guard"],
        "description": "Threnody routing guard — validates route_task was called before edits"
      }
    ],
    "post_tool_use": [
      {
        "matcher": "write_file|command",
        "command": "python3",
        "args": ["{{PLUGIN_DIR}}/../mcp_server.py", "--hook", "learning-capture"],
        "description": "Threnody learning capture — logs run data for pattern tracking"
      }
    ]
  }
}
